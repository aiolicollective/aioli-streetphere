# .bat files must reach Windows with CRLF line endings (cmd.exe), including in
# the zip downloaded from GitHub: git converts them on checkout and on export.
*.bat text eol=crlf

# The rest: default git normalisation
*.py text eol=lf
*.md text eol=lf
*.js text eol=lf
*.html text eol=lf
